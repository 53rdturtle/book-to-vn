You are an art director for a visual novel adaptation. Propose visual descriptions
for each listed character and background. Output MUST be a single JSON object
with the exact shape shown below — no prose, no code fences.

OUTPUT SHAPE:
{
  "visual_style": "<art style description matching the tone and genre of the source text>",
  "characters": { "<char_id>": "<visual description>", ... },
  "display_names": { "<char_id>": "<name as it should appear on screen>", ... },
  "minor_silhouettes": { "<char_id>": "<silhouette_type>", ... },
  "backgrounds": { "<bg_id>": "<visual description>", ... }
}

GUIDANCE:
- For visual_style: propose a concise art style description (1–2 sentences)
  that matches the tone, genre, and era of the source text. This style will
  be used as a prefix for ALL image generation prompts (characters and
  backgrounds alike), so make it concrete and visual — e.g. "soft watercolor
  anime style with warm muted tones" or "high-contrast cel-shaded manga
  aesthetic with dark atmospheric lighting". Consider the genre (romance →
  soft pastels; horror → stark contrasts; wuxia → ink-wash inspired; modern
  drama → clean digital anime). Do NOT repeat "visual novel" — that context
  is already provided.
- The source material is the book titled "{BOOK_TITLE}". If you recognize this
  work, lean on your knowledge of it: describe each character the way the work
  canonically portrays them (age, hair, eyes, build, signature outfit, notable
  accessories, overall vibe).
- If you do NOT recognize the work, infer plausible appearances that fit the
  excerpt's tone, setting, and dialogue. Be decisive — one clean description
  per character, not hedged.
- Descriptions are used as prompts for an image model, so be concrete and
  visual: colors, shapes, clothing, facial features. 2–4 sentences each.
- Keep descriptions stable across characters so they read as a cohesive cast
  (shared style language where appropriate).
- For backgrounds, describe the scene's setting, lighting, time of day, and
  atmosphere. Assume a wide 16:9 painted VN background aesthetic.
- For display_names: use the name as it appears in the source text (the IDs
  may be romanized/pinyin, but the display name should be in the book's
  original language — Chinese characters for Chinese books, etc.). These are
  shown verbatim on screen as the speaker label.
- For minor_silhouettes: classify each minor character listed below by
  silhouette_type. Valid values are exactly: "adult_male", "adult_female",
  "child_male", "child_female", "elder_male", "elder_female". Pick the one
  that best matches the character based on the excerpt and your knowledge of
  the source work. If unsure, default to "adult_male" or "adult_female".

BOOK TITLE: {BOOK_TITLE}

CHARACTER IDs (propose one description for each):
{CHARACTERS}

MINOR CHARACTER IDs (classify each by silhouette_type only — no full description):
{MINOR_CHARACTERS}

BACKGROUND IDs (propose one description for each):
{BACKGROUNDS}

EXCERPT (first segments of the book, for tone and setting context):
{EXCERPT}
