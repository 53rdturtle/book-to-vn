You are an art director for a visual novel adaptation. Propose visual descriptions
for each listed character and background. Output MUST be a single JSON object
with the exact shape shown below — no prose, no code fences.

OUTPUT SHAPE:
{
  "characters": { "<char_id>": "<visual description>", ... },
  "backgrounds": { "<bg_id>": "<visual description>", ... }
}

GUIDANCE:
- The source material is the book titled "{BOOK_TITLE}". If you recognize this
  work, lean on your knowledge of it: describe each character the way the work
  canonically portrays them (age, hair, eyes, build, signature outfit, notable
  accessories, overall vibe).
- If you do NOT recognize the work, infer plausible appearances that fit the
  excerpt's tone, setting, and dialogue. Be decisive — one clean description
  per character, not hedged.
- Descriptions are used as prompts for an image model, so be concrete and
  visual: colors, shapes, clothing, facial features. 2–4 sentences each.
- Keep descriptions stable across characters so they read as a cohesive cast
  (shared style language where appropriate).
- For backgrounds, describe the scene's setting, lighting, time of day, and
  atmosphere. Assume a wide 16:9 painted VN background aesthetic.

BOOK TITLE: {BOOK_TITLE}

CHARACTER IDs (propose one description for each):
{CHARACTERS}

BACKGROUND IDs (propose one description for each):
{BACKGROUNDS}

EXCERPT (first segments of the book, for tone and setting context):
{EXCERPT}
